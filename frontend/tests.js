// donationPercent() returns Promise "x%"
// payAndDonate(percent, donee, ether) returns Promise ...
// proportionToPercent(random proportions) return correct percents
// percentToProportion(random percents) return correct proportions
